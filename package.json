{
  "name": "cue-h0-hackathon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @cue-h0/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @cue-h0/db db:generate",
    "db:migrate": "pnpm --filter @cue-h0/db db:migrate",
    "db:seed": "pnpm --filter @cue-h0/db db:seed"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.30.0"
}
